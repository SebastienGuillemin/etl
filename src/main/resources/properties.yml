sql:
  query: SELECT e.* FROM echantillon e where e.id_composition is not null order by created_at  desc limit 1000

ontology:
  base:
    prefix: http://www.stups.fr/ontologies/2023/stups/#
    name: STUPS.ttl
  save:
    name: populated_STUPS.ttl
    estProcheChimiquementDe: false
    estProcheDe: false