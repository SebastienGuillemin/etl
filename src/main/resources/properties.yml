sql:
  query: select e.* from echantillon e inner join count_produit_coupage_par_echantillon pc_per_e on e.id = pc_per_e.echantillon_id where count_pc <= 2

ontology:
  base:
    prefix: http://www.stups.fr/ontologies/2023/stups/#
    name: STUPS.ttl
  save:
    name: populated_STUPS_cannabis.ttl
    estProcheChimiquementDe: True
    estProcheDe: True